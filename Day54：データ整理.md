

Day54 完全まとめ


■ 今日やったこと

コンディションデータの「整理」を実装

選手IDで絞り込み（filter）
日付で並び替え（sort）
表示を用途に合わせて最適化


■ 実装の核心

① データの流れ
filter → sort → 表示

② 並び替え処理（最重要）
.sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime())


■ 理解（本質）

① sortの正体
.sort((a, b) => 数値)

👉 2つを比較して順番を決める


② ルール
マイナス → aが前
プラス → bが前


③ 日付の扱い
new Date().getTime()

👉 日付 → 数値に変換して比較


④ なぜ新しい日付が上に来るか

b - a
新しい日付 → 数値が大きい
結果 → bが前に来る

👉 最新順になる



■ UIの判断

情報量が少ない → 横表示が最適
一覧性・スピード重視

👉 今回は横で正解